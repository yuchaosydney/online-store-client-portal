// import { fromJS } from 'immutable';
// import { selectProductListContainerDomain } from '../selectors';

describe('selectProductListContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
